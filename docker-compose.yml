version: '3.8'
services:
  app:

    image: backend-pis-6to:1.2 # Cambia esto por el nombre de tu imagen si ya la has construido
    build:
      context: .
    env_file:
      - .env
    ports:
      - "${PORT}:${PORT}" # Mapea el puerto del contenedor al puerto definido en el .env